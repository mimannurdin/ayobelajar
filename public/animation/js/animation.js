var animation = bodymovin.loadAnimation({
  container: document.getElementById('motion-home'),
  renderer: 'svg',
  loop:true,
  autoplay:true,
  path:'/animation/json/home.json'
})

var animation = bodymovin.loadAnimation({
  container: document.getElementById('motion-about'),
  renderer: 'svg',
  loop:true,
  autoplay:true,
  path:'/animation/json/about.json'
})
var animation = bodymovin.loadAnimation({
  container: document.getElementById('loading'),
  renderer: 'svg',
  loop:true,
  autoplay:true,
  path:'/animation/json/rocket loading.json'
})
var animation = bodymovin.loadAnimation({
  container: document.getElementById('loading1'),
  renderer: 'svg',
  loop:true,
  autoplay:true,
  path:'/animation/json/rocket flayaway.json'
})
var animation = bodymovin.loadAnimation({
  container: document.getElementById('bg'),
  renderer: 'svg',
  loop:true,
  autoplay:true,
  path:'/animation/json/sky-space.json'
})
var animation = bodymovin.loadAnimation({
  container: document.getElementById('confetti'),
  renderer: 'svg',
  loop:true,
  autoplay:true,
  path:'/animation/json/confetti.json'
})